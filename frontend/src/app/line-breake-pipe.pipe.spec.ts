import { LineBreakePipePipe } from './line-breake-pipe.pipe';

describe('LineBreakePipePipe', () => {
  it('create an instance', () => {
    const pipe = new LineBreakePipePipe();
    expect(pipe).toBeTruthy();
  });
});
